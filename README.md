A small command line program for Windows to keep track of tasks and deadlines.

Each task contains a task string, date added, date due and up to 5 tags (groups). 

Currently commands allow addition, deletion, modification and listing of tasks according to supplied arguments as well as a command to undo the last change.

Testbench included to test correctness of operation after any change to the program itself.

See LICENSE.txt for permissions.
